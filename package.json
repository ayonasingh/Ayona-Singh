{
  "name": "ayona-portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && cd ../react-portfolio && npm install && npm run build",
    "vercel-build": "cd backend && npm install && cd ../react-portfolio && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.x"
  }
}
