{
  "name": "ayona-portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd react-portfolio && npm install && npm run build",
    "dev:backend": "cd backend && npm start",
    "dev:frontend": "cd react-portfolio && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../react-portfolio && npm install"
  },
  "engines": {
    "node": ">=18.x"
  }
}
